server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test_db
  flyway:
    enabled: false
  jpa:
    show-sql: false
    database: h2
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true